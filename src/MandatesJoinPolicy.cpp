#include "../include/JoinPolicy.h"
#include "../include/Party.h"
#include "../include/Agent.h"

void MandatesJoinPolicy::join(vector<Offer> offs, Party &p) {
    //p.setState(Joined);

    // clone agent
    //Agent clonedAgn = new Agent(agn.getId(), agn.getPartyId(), agn.????????)
}
